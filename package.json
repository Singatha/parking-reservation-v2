{
  "name": "parking-reservation-v2",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w @parking/api\" \"npm run dev -w @parking/web\"",
    "build": "npm run build -w @parking/web",
    "lint": "npm run lint -w @parking/api && npm run lint -w @parking/web",
    "test": "npm run test -w @parking/api",
    "test:integration": "npm run test:integration -w @parking/api",
    "test:e2e": "playwright test",
    "test:all": "npm run test && npm run test:integration && npm run test:e2e",
    "db:migrate": "npm run db:migrate -w @parking/api",
    "db:seed": "npm run db:seed -w @parking/api"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "concurrently": "^9.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
